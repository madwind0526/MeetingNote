# Code Review - 2026-09-03

## 결과 요약

리뷰에서 발견한 4개 항목을 모두 수정했습니다.

1. 긴 회의 원본 오디오 저장 제한 불일치 수정
   - 클라이언트 업로드 제한을 자료 50MB, 오디오 200MB로 분리했습니다.
   - 서버의 오디오 첨부 허용 크기와 클라이언트 검증이 어긋나지 않도록 맞췄습니다.

2. 회의록 수정/삭제 권한 보강
   - 목록/상세 화면의 수정 버튼을 작성자 또는 관리자에게만 보이도록 조정했습니다.
   - `/api/meetings` 생성/수정/삭제와 댓글 생성/삭제에 서버 인증 및 권한 검사를 추가했습니다.

3. `..`가 포함된 정상 파일명 첨부 열기 실패 수정
   - `draft..v2.pdf` 같은 정상 파일명은 허용하되, 실제 경로 세그먼트가 `..`인 traversal 시도만 차단하도록 변경했습니다.
   - 최종 `path.resolve()` 결과가 첨부 폴더 밖으로 벗어나지 않는지도 추가로 검증합니다.

4. 회원/게시판/설정성 API 권한 검증 및 주석 정리
   - 로그인 세션 토큰 기반 서버 인증을 추가했습니다.
   - 회원 관리, 게시판 저장, 사전 적용, 로고/환경 설정, 음성 프로필 등 쓰기 API에 권한 검사를 붙였습니다.
   - 코드 주석의 비영어/깨진 문자열을 정리해 G-01 규칙을 맞췄습니다.

## 검증

- `npm run build` 통과
- 코드 주석 한글/깨진 비ASCII 주석 스캔 통과
- Vite 번들 크기 경고는 기존과 동일하게 남아 있습니다.

## 참고

- 기존 localStorage 로그인 세션은 서버 토큰이 없으므로 다시 로그인해야 합니다.
- 로그인 건너뛰기는 `MEETINGNOTE_ALLOW_SKIP_LOGIN=true` 환경에서만 허용되도록 막았습니다.
